> Archived document.
> This file is kept for historical context only and may not describe the current Nodo implementation.

# Archived Cycle Notes

These files document earlier implementation cycles. They are kept for history
and design traceability, but the stable project docs live at the top of
`docs/`.
